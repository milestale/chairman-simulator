/*if (mouse_check_button(mb_left) && position_meeting(mouse_x, mouse_y, other))
	{	
		show_flyout = 0;
	}
if (mouse_check_button(mb_left) && position_meeting(mouse_x, mouse_y, self))
	{	
		show_flyout = 1;
	}

